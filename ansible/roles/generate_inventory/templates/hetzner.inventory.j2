[masters]
{{ vm_name | default(ip_address) }} ansible_user={{ username | default('root') }} ansible_ssh_private_key_file={{ ssh_private_key_path }} public_ip_address={{ ip_address }} ansible_ssh_host={{ ip_address }}

apiVersion: v1
kind: List
items:
- apiVersion: v1
  kind: DeploymentConfig
  metadata:
    generation: 1
    labels:
      app: launcher-keycloak
    name: launcher-keycloak
  spec:
    replicas: 1
    revisionHistoryLimit: 10
    selector:
      app: launcher-keycloak
      deploymentconfig: launcher-keycloak
    strategy:
      activeDeadlineSeconds: 21600
      resources: {}
      rollingParams:
        intervalSeconds: 1
        maxSurge: 25%
        maxUnavailable: 25%
        timeoutSeconds: 600
        updatePeriodSeconds: 1
      type: Rolling
    template:
      metadata:
        labels:
          app: launcher-keycloak
          deploymentconfig: launcher-keycloak
      spec:
        containers:
        - image: docker.io/quintesse/launcher-keycloak:latest
          imagePullPolicy: Always
          name: launcher-keycloak
          ports:
          - containerPort: 8080
            protocol: TCP
          resources: {}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
        dnsPolicy: ClusterFirst
        restartPolicy: Always
        schedulerName: default-scheduler
        securityContext: {}
        terminationGracePeriodSeconds: 30
    test: false
    triggers:
    - type: ConfigChange
- apiVersion: v1
  kind: Service
  metadata:
    labels:
      app: launcher-keycloak
    name: launcher-keycloak
  spec:
    ports:
    - name: 8080-tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
    selector:
      app: launcher-keycloak
      deploymentconfig: launcher-keycloak
    sessionAffinity: None
    type: ClusterIP
  status:
    loadBalancer: {}
- apiVersion: v1
  kind: Route
  metadata:
    labels:
      app: launcher-keycloak
    name: launcher-keycloak
  spec:
    host:
    port:
      targetPort: 8080-tcp
    to:
      kind: Service
      name: launcher-keycloak
      weight: 100
    wildcardPolicy: None

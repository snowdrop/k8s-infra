---
- name: "Check the passwordstore installation"
  hosts: localhost
  gather_facts: no

  tasks:
  - name: "Check passwordstore installation"
    include_role:
      name: "passstore/check"
    tags: [always]
...

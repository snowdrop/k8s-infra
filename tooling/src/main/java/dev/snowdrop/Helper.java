package dev.snowdrop;

import dev.snowdrop.type.Config;
import dev.snowdrop.type.Role;

import java.util.HashMap;

public class Helper {
    public static HashMap<Integer, Role> roles = new HashMap<Integer, Role>();
    public static Config cfg = new Config();
}
